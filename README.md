# look-bot

Telegram-бот: пользователь присылает своё фото — получает инфографику
«ЛУЧШИЙ ОБРАЗ» 4:5. Цветотип, палитра, брови, контуринг, три ракурса причёски и
пять вариантов макияжа — всё на одном листе, собранном из сгенерированных
фрагментов его собственного лица.

## Запуск

```
pip install -r requirements.txt
```

Дальше `python bot/bot.py`. Перед этим скопируй ключи в `.env` — нужны kie.ai
(агент и nano-banana), imgbb (хостинг референсов) и токен Telegram.

Прогнать конвейер без бота, на локальном файле:

```
python tests/run_full.py
```

## Два режима

**Разбор.** Агент-колорист смотрит портрет и возвращает честный анализ:
подтон кожи, цветотип, что идёт и что нет. Ответ — не свободный текст, а JSON
по строгой схеме (`colorist/schema.py`, pydantic): фиксированные длины списков,
лимиты на длину строк, hex-формат цветов. Если модель промахнулась мимо схемы,
ответ отбраковывается и перезапрашивается — иначе вёрстка развалится.

**Примерочная.** Вместо честного разбора берётся образ из каталога —
«хочу как вот эта» — и `style_agent` подгоняет чужой образ на лицо
пользователя.

## Как собирается лист

Смысл в том, что **макет — это код, а не картинка из модели**. Модель рисует
только то, что обязана: пять фотослотов с лицом. Заголовки, палитры, подписи,
плашки, сетка — всё отрисовано в Pillow, поэтому текст всегда резкий, кириллица
не ломается и цвета в палитре ровно те, что вернул агент.

`template/build_template.py` — единственный источник правды по координатам.
Дизайн-канвас 2048×2560 (4:5), экспорт ×2 — 4096×5120. Все рамки, кропы и слоты
живут в одной системе координат, поэтому область, сгенерированная nano-banana,
кропается и вклеивается обратно ровно в те же пиксели. Скрипт выдаёт:

* `template_annotated.png` — человеческий референс со всеми слотами и размерами
* `template_nano_clean.png` — только рамки, чёрным по белому, это уходит в модель
* `nano_hair3.png` / `nano_eyes5.png` — отдельные кадры на 3 и 5 ячеек
* `crops.json` — точные прямоугольники для кропа и вклейки

Меняешь макет — перегоняешь этот скрипт, и рендер подхватывает новые
координаты сам.

```
look-bot/
├── bot/         aiogram: приём фото, статус, выдача результата
├── colorist/    агент, схема ответа, сборка ассетов, монтаж, прогресс
├── template/    build_template.py — координаты макета
├── render/      финальная отрисовка листа в Pillow
├── catalog/     каталог образов для примерочной
└── tests/       прогоны конвейера на фикстурах
```

## Прогресс

Генерация долгая — несколько вызовов модели подряд. `colorist/progress.py`
ведёт статус по этапам (анализ → ассеты → монтаж) и обновляет одно сообщение в
чате, вместо того чтобы молчать три минуты. `colorist/stats.py` копит время и
стоимость прогонов.
